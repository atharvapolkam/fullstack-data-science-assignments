#!/usr/bin/env python
# coding: utf-8

# In[ ]:


get_ipython().set_next_input('1. To what does a relative path refer');get_ipython().run_line_magic('pinfo', 'refer')
 the current working directory


# In[ ]:


get_ipython().set_next_input('2. What does an absolute path start with');get_ipython().run_line_magic('pinfo', 'with')
the root folder, such as / or C:


# In[ ]:


get_ipython().set_next_input('3. What do the functions os.getcwd() and os.chdir() do');get_ipython().run_line_magic('pinfo', 'do')
 os.getcwd() returns the current working directory os.chdir() function changes the current working directory


# In[ ]:


get_ipython().set_next_input('4. What are the . and .. folders');get_ipython().run_line_magic('pinfo', 'folders')
Answer: . folder is the current folder, and .. is the parent folder.


# In[ ]:


5. What are the . and .. folders? 
Answer: . folder is the current folder, and .. is the parent folder.


# In[ ]:


get_ipython().set_next_input('6. What are the three “mode” arguments that can be passed to the open() function');get_ipython().run_line_magic('pinfo', 'function')
Answer: 'r' for read mode, 'w' for write mode, 'a' for append mode


# In[ ]:


get_ipython().set_next_input('7.What happens if an existing ﬁle is opened in write mode');get_ipython().run_line_magic('pinfo', 'mode')
Answer: is erased and completely overwritten


# In[ ]:


8.What is the difference between the read() and readlines() methods? 
Answer: read() returns the ﬁle’s entire contents as a single string value, readlines() returns a list of strings,
where each string is a line from the ﬁle’s contents.


# In[ ]:


get_ipython().set_next_input('9.What is the difference between the read() and readlines() methods');get_ipython().run_line_magic('pinfo', 'methods')
Answer: read() returns the ﬁle’s entire contents as a single string value, readlines() returns a list of strings,
    where each string is a line from the ﬁle’s contents.

